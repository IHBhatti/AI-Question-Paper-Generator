# Core framework
streamlit>=1.37.0

# LangChain RAG stack
langchain>=0.2.14
langchain-core>=0.2.38
langchain-text-splitters>=0.2.2
langchain-community>=0.2.12
langchain-huggingface>=0.0.3
langchain-groq>=0.1.9

# Vector database (free, local)
faiss-cpu>=1.8.0

# Embeddings
sentence-transformers>=3.0.1

# PDF parsing
PyMuPDF>=1.24.9

# OCR for scanned PDFs (e.g. textbook-board scans with no text layer)
pytesseract>=0.3.10
Pillow>=10.4.0
# NOTE: pytesseract requires the Tesseract OCR *engine* to be installed
# separately (it is not a pip package). See README for install commands.

# PDF generation
reportlab>=4.2.2

# Utilities
python-dotenv>=1.0.1
